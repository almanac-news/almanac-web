# circle.yml
machine:
  node:
    version: 5.0.0

dependencies:
  pre:
    - sudo apt-get install build-essential g++
    - rm -rf node_modules/

test:
  override:
    - npm run lint
    - npm test

notify:
  webhooks:
    - url: https://webhooks.gitter.im/e/c2658ec4ce7f0836941c
